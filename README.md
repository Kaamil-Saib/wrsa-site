# wrsa-site
cd into with "cd wrsa-site"
run "firebase deploy --only functions" in the terminal then
run "npm run dev"

or rather

run "npm run build && firebase deploy --only hosting" to host and copy the url into a browser